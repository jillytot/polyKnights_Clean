using UnityEngine;
using System.Collections;

public class lookAtTarget : MonoBehaviour {

	//Let's make the camera rotate to look at my leader. 
	//Transform lookAtThis;
	//Quaternion myRotation;
	//public float turnSpeed = 0.2f;

	// Use this for initialization
//	void Start () {
//
//		lookAtThis = transform.parent.GetComponent<cameraFX>().myLeader;
//		myRotation = transform.rotation;
//	
//	}
//	
//	// Update is called once per frame
//	void Update () {
//
//		if (lookAtThis) {
//
//			var lookXZ = new Vector3(lookAtThis.transform.position.y, 0, lookAtThis.transform.position.z);
//
//			var targetRotation = Quaternion.LookRotation(lookAtThis.transform.position - transform.position, transform.up); //set target towards direction of motion
//			transform.rotation = transform.rotation.EaseTowards(targetRotation, turnSpeed); //rotate towards the direction of motion
//			//var lockRot = 0;
//			//transform.rotation.eulerAngles.y = lockRot;
//			myRotation = transform.rotation;


		//}
	//}
}
